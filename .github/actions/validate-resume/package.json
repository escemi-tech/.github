{
  "name": "validate-resume",
  "version": "1.0.0",
  "private": true,
  "description": "Validates resume JSON files against the resume schema.",
  "dependencies": {
    "@jsonresume/schema": "^1.3.1",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  },
  "scripts": {
    "validate": "node ./validate.js"
  }
}
