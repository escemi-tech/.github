{
  "name": "generate-resume-pdf",
  "version": "1.0.0",
  "private": true,
  "description": "Generates PDF resumes from JSON resume files.",
  "dependencies": {
    "jsonresume-theme-escemi": "file:../../../resume/themes/escemi",
    "resume-cli": "^3.1.2"
  },
  "scripts": {
    "generate-pdf": "resume export --theme escemi --format pdf --resume"
  }
}
